{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "Run backend tests with color",
			"command": "pytest --maxfail=1 --disable-warnings --tb=short --color=yes",
			"group": "test"
		}
	]
}