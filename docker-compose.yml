version: '3'

services:
  portfolio:
    build:
      context: ./app
      dockerfile: Dockerfile
    restart: always
    ports:
      - "8080:8080"
    volumes:
      - ./app/nginx.conf:/etc/nginx/conf.d/default.conf
