services:
  portfolio:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: always
    expose:
      - "8080"
    networks:
      - web
    volumes:
      - ./frontend/nginx.conf:/etc/nginx/conf.d/default.conf

networks:
  web:
    external: true
