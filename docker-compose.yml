services:
  portfolio:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: always
    ports:
      - "8080:8080"
    networks:
      - web

networks:
  web:
    external: true
    name: web
