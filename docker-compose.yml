version: '3.9'
services:
  web:
    build: .
    command: >
      sh -c "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn -c gunicorn.conf.py root.wsgi:application"
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    env_file:
      - .env
