version: '3.9'
services:
  web:
    build: .
    command: >
      sh -c "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn root.wsgi:application --bind 0.0.0.0:8080"
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    env_file:
      - .env
